name: smart_energy
description: Smart Energy mobile — energie IoT Grand Tunis — El Jezi Ghassen
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mqtt_client: ^10.3.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sites.json
    - assets/demo_snapshot.json
    - assets/demo_history.json
